version: '3.8'

services:
  postgres:
    image: postgres:12
    container_name: postgres-db
    env_file:
      - .env
    volumes:
      - "./postgresql:/postgres_init"
      - pg_data:/var/lib/postgresql/data
volumes:
  pg_data: